# Frontend runtime environment variables (to be injected during deployment/build)
# The orchestrator will map and inject these into the Angular app via index.html or build-time replacement.

# PUBLIC_INTERFACE
# API base URL for backend REST endpoints. Example: https://api.example.com
NG_APP_API_BASE_URL=https://api.example.com

# PUBLIC_INTERFACE
# Optional: Web app site URL (used by email redirect flows if needed in future)
NG_APP_SITE_URL=https://app.example.com
